import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Toaster } from "@/components/ui/toaster";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "AttentionX - Turn Long Videos into Viral Clips with AI",
  description:
    "Automated content repurposing engine that transforms long-form videos into viral short-form clips using AI-powered emotional peak detection, smart cropping, and dynamic captions.",
  keywords: [
    "AttentionX",
    "video repurposing",
    "viral clips",
    "AI video editing",
    "short-form content",
  ],
  authors: [{ name: "AttentionX" }],
  icons: {
    icon: "/logo.svg",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="dark" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased bg-background text-foreground`}
      >
        {children}
        <Toaster />
      </body>
    </html>
  );
}
